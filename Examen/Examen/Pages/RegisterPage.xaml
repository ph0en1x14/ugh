<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Examen.Pages.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Examen.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel
        Width="320"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
        Spacing="12">

        <TextBlock Text="Register" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>

        <TextBox x:Name="NameBox"     PlaceholderText="Full Name" />
        <TextBox x:Name="UsernameBox" PlaceholderText="Username" />
        <TextBox x:Name="EmailBox"    PlaceholderText="Email" />
        <PasswordBox x:Name="PasswordBox" PlaceholderText="Password" />

        <TextBlock x:Name="MessageText" Visibility="Collapsed" />

        <Button Content="Register" Click="RegisterButton_Click"
                HorizontalAlignment="Stretch" Background="#2ECC71" Foreground="White"/>

        <Button Content="Back to Login" Click="BackButton_Click"
                HorizontalAlignment="Stretch" />

    </StackPanel>
</Page>
